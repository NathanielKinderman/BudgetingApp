@model IEnumerable<BudgetingApp.Models.CreateEvent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Planner.EmailAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.City)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOfEvent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberOfMembers)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TheBudgetOfEvent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Planner.EmailAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.City)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfEvent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumberOfMembers)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TheBudgetOfEvent)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EventsName }) |
            @Html.ActionLink("Details", "Details", new { id=item.EventsName }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EventsName })
        </td>
    </tr>
}

</table>
